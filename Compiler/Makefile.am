# Only necessary if AC_CONFIG_MACRO_DIR is on in configure.ac
# ACLOCAL_AMFLAGS = -I m4

SUBDIRS = inc lib src

EXTRA_DIST = programs 

# Install into the doc directory
doc_DATA = COPYING INSTALL README.md
